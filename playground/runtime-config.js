// Default backend URL the playground/voice/RAG pages talk to.
// Overridable from the "API keys" dialog (persisted to localStorage).
window.VOICE_API_BASE = "http://localhost:8100";
window.BHUKKAD_API_BASE = window.VOICE_API_BASE;

